#!/usr/bin/env node

// Test file with shebang
const toolName = "Claude Code";
const version = "2.0.0";

const welcomeMessage = `Welcome to ${toolName} version ${version}!
Use --help for more information.`;

console.log(welcomeMessage);